import { PromotionCard } from "@/components/ui/cards/promotion-card";

type Props = {};

const Promotion = ({}: Props) => {
  return (
    <div className="section md">
      <PromotionCard />
    </div>
  );
};

export { Promotion };
